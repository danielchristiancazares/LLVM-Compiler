/*
 * Symbol table implementation
 *
 */

#include "symtable.h"

void SymbolTable::PrintTable () {
//	int symbolCount = 0;
//	int scopeCount = 0;
//	for(vector< map<string, Decl*> >::iterator it = this->symTable.begin(); it != this->symTable.end(); ++it) {
//		scopeCount += 1;
//		map<string, Decl*> currMap = *it;
//		symbolCount = 0;
//		for(map<string, Decl*>::iterator it2 = currMap.begin(); it2 != currMap.end(); ++it2) {
//			symbolCount += 1;
//		}
//	}
}